 
export const mappers = () => {  

  // #region SYSTEM SETTINGS 
  const mapPaymentChannels = (channels: any)  => { 
    let paymentChannelsWithPath: any[] = []; 
    return paymentChannelsWithPath;
  };
  // #endregion

  return { 
    mapPaymentChannels
  };
};
